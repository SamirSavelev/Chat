import { FC } from "react";
import { NewMessageStyles } from "./styles";

const { Container } = NewMessageStyles;

const NewMessage: FC = () => {
  return <Container>Новые сообщения</Container>;
};

export default NewMessage;
